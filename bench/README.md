# bench — fc が生成するコードのベンチマーク

最適化の効果と退行を、決定的な数字（エミュレータのサイクル数とコードサイズ）で追うためのプログラム群。

```bash
go test ./bench            # results.json と比べる（出力・サイクル数・サイズ）
go test ./bench -v         # 一覧表を表示
go test ./bench -update    # 現在の値で results.json を書き換える（意図した変化のとき）
```

## 仕組み

- 各 `*.fc` は emu ターゲットで動く独立したプログラム。処理本体を `bench_start()` / `bench_end()`（`fclib/emu/stdio.fc`）で
  囲み、最後にチェックサムを `printf` して `exit(0)` する
- サイクル数はエミュレータ（`internal/r6502`）が数える。ページクロスと分岐成立のペナルティ込みで、同じバイナリなら必ず同じ値
- サイズは ld65 の map から、そのモジュールのセグメント（コード + ROM 上の定数）のバイト数
- `results.json` が基準値。出力が変わったらコンパイラのバグ（テスト失敗）、サイクル数・サイズが変わったら
  最適化の効果か退行（`-update` で受け入れる。golden と同じ運用）
- 各プログラムの答えは Python などで同じ計算を再現して照合してある（新しいベンチを足すときも同じようにする）

## プログラム

### A. 移植ベンチ（他のコンパイラの公開結果と比べられる）

[c-bench-64](https://github.com/thred/c-bench-64) / [millfork-benchmarks](https://github.com/KarolS/millfork-benchmarks) の
アルゴリズムを fc に移した。`sieve` / `crc8` / `crc16` は c-bench-64 と同じ規模なので、公開されている秒数と直接比べられる
（下の「他のコンパイラとの概算比較」）。

| 名前 | 内容 | 何を測るか |
|---|---|---|
| `sieve` | エラトステネスの篩、8191 フラグ × 10 回（c-bench-64 の sieve.c と同一） | ポインタを進めるループ、16 ビットの比較・加算 |
| `crc8` | CRC-8/GSM-A（テーブル無し）、8192 バイト（c-bench-64 の crc8.c と同一） | 1 バイトのシフト・XOR・分岐 |
| `crc16` | CRC-16/XMODEM、8192 バイト（c-bench-64 の crc16.c と同一） | 16 ビットのシフト・XOR・比較 |
| `plasma` | cc65 samples/plasma.c 由来、32x30 × 50 フレーム | sin テーブル引き + バイト加算 + 行ポインタ更新 |
| `linkedlist` | 200 ノードの単方向リストの走査 × 20 回 | struct のポインタ経由フィールド参照 |
| `fib` | 再帰 fib(18)、約 8,000 呼び出し | 関数呼び出し・フレーム確保・戻り値 |

### B. ゲーム形ベンチ（castle の処理を模したもの）

castle（`examples/castle`）のホットパスと同じ形の処理。数字の意味はこちらの方が重い。

| 名前 | 相当する castle のコード | 内容 |
|---|---|---|
| `entities` | `en.process` | SoA の 32 体を毎フレーム: 状態機械 → 移動 → タイルマップ衝突で反射 → アニメ。100 フレーム |
| `bgdecode` | `bg.fetch_area` / `scroll` | RLE の列を展開 → 属性テーブル → PPU 転送バッファ。64 列 × 4 周 |
| `oam` | `my.draw` / en の描画 | 2x2 メタスプライト（反転あり）24 個から OAM 256 バイトを構築。100 フレーム |
| `textprint` | `text.print` / `print_num` | 文字表で変換しながら幅で折り返し、16 ビット値の 10 進化。200 回 |
| `calls` | en_vtbl の `PROCESS[t](i)` など | 通常呼び出し / fastcall / 関数ポインタ表 / 4 段の入れ子、各 500 回 |
| `math16` | `my.fc` / `boxelev.fc` の物理 | 8.8 固定小数の位置・速度・重力・跳ね返り、距離、定数の乗除。64 個 × 100 フレーム |

## castle の 1 フレーム（マクロベンチ）

`go test ./internal/nes -run CastleFrameCycles -v`。examples/castle を内蔵 NES ランナーで自動プレイ（タイトル →
A で開始 → フィールドで右移動とジャンプ。入力の時刻は固定）し、フレームごとの **busy サイクル = フレーム長（29,868）−
vsync 待ちループ（`while (vsync_flag) {}` の `lda; bne`）の時間** を局面ごとに集計して `castle_frames.json` と比べる。
NMI の中の仕事（VRAM 転送など）も busy に入る。`over` は待ちループに入らなかった（1 フレームに収まらなかった）フレーム数。
ゲーム形ベンチ 9 本が拾えない「実ゲームの重さ」を見るためのもので、意図した変化は `-update` で受け入れる。
`-v` ではフィールドの局面の**関数別の内訳**（ld65 のマップのシンボル区間に命令のサイクルを積んだもの。上位 25）も出る。

## 他のコンパイラとの概算比較（2026-09-15）

c-bench-64 の公開結果（C64 実機相当の VICE、TOD クロックで 0.1 秒単位）と、fc のサイクル数を C64 の
クロック 0.985 MHz（PAL）で秒に直したもの。向こうは cc65 2.19 `-Oisr --static-locals`、llvm-mos `-O2 -flto`、
Oscar64 1.32 `-O3`、vbcc `-O4 -speed`、SDCC 4.5 `--opt-code-speed`（2026-03 の値）。CRC のデータが違う
（Kernal ROM vs 擬似乱数）が、分岐の成否にしか効かないので数 % 以内。

| bench | fc v0.0.2 | **fc 2026-09-16** | cc65 | llvm-mos | Oscar64 | vbcc | SDCC |
|---|---:|---:|---:|---:|---:|---:|---:|
| `sieve` | 36.1 s | **18.4 s** | 20.5 s | 18.8 s | 8.7 s | 13.5 s | 20.8 s |
| `crc8` | 4.2 s | **1.37 s** | 3.2 s | 1.0 s | 0.4 s | 1.2 s | 1.2 s |
| `crc16` | 9.2 s | **2.55 s** | 4.3 s | 1.7 s | 1.6 s | 2.6 s | 2.6 s |

最適化前の v0.0.2 は **cc65 より 1.3〜2.2 倍遅く、Oscar64 より 4〜10 倍遅かった**。2026-09-16 の最適化第 1 弾
（下の「経過」）で cc65 は抜き、llvm-mos に並んだ。Oscar64 とはまだ 2〜6 倍。第 2 弾はこの 3 本にはほとんど効かない
（対象が switch / 条件 / SoA / 呼び出しなので。ゲーム形ベンチの方を見る）。

v0.0.2 の生成コードを見ると原因ははっきりしていて、どれも IR レベルの最適化が無いことによる
（`.fc-build/_sieve.s` の内側ループが 42 命令 ≒ 120 サイクル。手書きなら 40）:

1. **一時変数経由のコピー**: `add $12 = a + b; load a = $12` を `a` に直接足さず、`L+12` に書いてから `L+8` に写す
   （2 バイトなら 4 命令 = 12 サイクルの無駄）。加算・減算の直後の代入で毎回起きる
2. **ポインタ参照のたびに `reg` へコピー**: `L` は ZP なのに `lda L; sta reg; lda L+1; sta reg+1; ... (reg),y`。
   `(L+8),y` と直接書けば 4 命令減る
3. **ジャンプの連鎖**: `jmp @end_27` → `@end_27: jmp @begin_23`、ループ末尾の `jmp` → 先頭の比較 → `jmp @else`
   （ループ 1 周に 3 つの `jmp` = 9 サイクル）。条件を反転して末尾で `bcc @begin` にすれば 1 つ
4. **A に残っている値の再ロード**: `sta L+4; lda L+4`、比較の直前の `lda`
5. 8 ビットのループ変数 `j` を毎回 `lda; sbc #1; sta` する（`dec L+3` で済む）

1・2・4 は「A/X/Y と直前に書いた場所の追跡」、3・5 はピープホール。Oscar64 の水準（`crc8` で 1 バイト 49 サイクル）は
crc を A に置いたまま `asl; bcc; eor` を回すレジスタ割付で、SSA と本格的な割付器が要る。

### 経過（2026-09-16、最適化第 1 弾。v0.0.2 → 現在）

| bench | v0.0.2 cycles | 現在 | | v0.0.2 size | 現在 | |
|---|---:|---:|---:|---:|---:|---:|
| `bgdecode` | 3,112,930 | 2,300,844 | -26% | 1724 | 1112 | -35% |
| `calls` | 341,681 | 272,397 | -20% | 631 | 497 | -21% |
| `crc16` | 9,110,810 | 3,245,624 | -64% | 676 | 327 | -52% |
| `crc8` | 4,178,215 | 2,401,063 | -43% | 488 | 277 | -43% |
| `entities` | 1,801,048 | 1,687,082 | -6% | 1721 | 1441 | -16% |
| `fib` | 748,304 | 748,304 | 0% | 169 | 166 | -2% |
| `linkedlist` | 499,336 | 334,855 | -33% | 654 | 554 | -15% |
| `math16` | 5,451,845 | 4,502,210 | -17% | 1619 | 1305 | -19% |
| `oam` | 5,303,727 | 3,692,344 | -30% | 1897 | 1462 | -23% |
| `plasma` | 6,221,809 | 4,580,740 | -26% | 707 | 533 | -25% |
| `sieve` | 35,532,746 | 18,280,785 | -49% | 503 | 332 | -34% |
| `textprint` | 7,700,371 | 5,863,692 | -24% | 1019 | 822 | -19% |

入れたもの（`internal/opt` = IR→IR、`internal/codegen` = 命令選択とピープホール）:

- opt: ジャンプの連鎖・直後への jump・到達不能ブロック・未参照ラベルの除去、分岐の反転、**ループの回転**（`if_true` 命令）
- opt: **一時変数経由のコピーの除去**（`op t = a, b; load x = t` → `op x = a, b`）、`x = (x op a) op b` の中間を x 自身に、
  2 バイト値のビット検査を 1 バイトに（`if (x & 0x8000)`）
- opt: ポインタ融合（`index`/`add` + `pget`/`pset`）を codegen から移し regalloc の前に。ポインタ + 添字も `ldy i; lda (p),y` に
- codegen: **ゼロページのポインタは `(p),y` で直接参照**（reg へのコピーを省く）、フレーム上のポインタの 1 バイト参照は `(S+n,x)`
- codegen: **A の値の追跡**で冗長な `lda` を消す（ゼロページの局所のみ。I/O レジスタは追跡しない）、`x ± 1` を `inc`/`dec`、
  定数シフトをメモリ上で（`asl lo; rol hi`、8 以上はバイト移動）、`x = x` を出さない
- regalloc: `index_pget` / `field_pget` / 定数シフトの結果も A に置ける

### 経過（2026-09-16、最適化第 2 弾。第 1 弾 → 現在）

`entities` の生成コードのレビューで見つけた無駄（switch、`&&`、SoA の書き込み、Y の重複…）を順に潰した。
最後の列は v0.0.2 からの累計。

| bench | 第 1 弾 cycles | 現在 | | 第 1 弾 size | 現在 | | v0.0.2 比 |
|---|---:|---:|---:|---:|---:|---:|---:|
| `bgdecode` | 2,300,844 | 1,954,723 | -15% | 1112 | 929 | -16% | -37% |
| `calls` | 272,397 | 266,397 | -2% | 497 | 489 | -2% | -22% |
| `crc16` | 3,245,624 | 3,229,227 | -1% | 327 | 316 | -3% | -65% |
| `crc8` | 2,401,063 | 2,401,060 | -0% | 277 | 275 | -1% | -43% |
| `entities` | 1,687,082 | 1,274,992 | -24% | 1441 | 897 | -38% | -29% |
| `fib` | 748,304 | 698,132 | -7% | 166 | 150 | -10% | -7% |
| `linkedlist` | 334,855 | 326,815 | -2% | 554 | 539 | -3% | -35% |
| `math16` | 4,502,210 | 4,335,342 | -4% | 1305 | 1203 | -8% | -20% |
| `oam` | 3,692,344 | 2,882,261 | -22% | 1462 | 1201 | -18% | -46% |
| `plasma` | 4,580,740 | 3,196,340 | -30% | 533 | 478 | -10% | -49% |
| `sieve` | 18,280,785 | 18,135,835 | -1% | 332 | 321 | -3% | -49% |
| `textprint` | 5,863,692 | 5,607,551 | -4% | 822 | 746 | -9% | -27% |

入れたもの:

- sema: **switch** の case 判定を値ごとに新しい一時変数で `eq t; if t goto next`（`cmp #k; bne`）に。**条件文脈の直接分岐化**
  （`compileCond`）: `if` / `while` の条件と値として使う `&&` / `||` を、`!` は向きの反転、`&&` / `||` は短絡の分岐、
  `!=` / `<=` / `>=` は `==` / `<` の反転にして、0 / 1 をメモリに作らない
- opt: **アドレス計算を使用位置へ寄せる**（`sinkAddress`。`a[i] += 1` の左辺のアドレスが右辺より先に出て融合できなかった）、
  `return a + b` / `return f(x)` の結果を戻り値の領域に直接置く
- regalloc: `pset` / `index_pset` / `field_pset` の書く値も A に置ける（ポインタの準備が A を壊すときは codegen が reg+2 に退避）
- codegen: ピープホールで **Y の値の追跡**（重複する `ldy`）、`lda` 直後の `cmp #0`、フレーム `<S+n,x` も正確な場所として追跡
  （X を変える命令で捨てる）、グローバル配列への `sta sym,y` はゼロページを壊さない扱い。**extend_jump を実サイズで固定点まで**
- 修正: `chainInPlace` が `x = (x << 1) ^ x` を壊していた（`internal/opt` に単体テストを追加）

### 経過（2026-09-16、第 3 弾: フレームの静的割付。第 2 弾 → 現在）

非再帰の関数のフレームを固定アドレス（`F_<sym>`、ゼロページ 64 バイト + RAM）に置き、`S+n,x` と `call` マクロをやめた
（[../doc/v2_frame_alloc.md](../doc/v2_frame_alloc.md) §6）。`fib` は再帰なので対象外（変化なし）。

| bench | 第 2 弾 cycles | 現在 | | 第 2 弾 size | 現在 | | v0.0.2 比 |
|---|---:|---:|---:|---:|---:|---:|---:|
| `bgdecode` | 1,954,723 | 1,887,637 | -3% | 929 | 895 | -4% | -39% |
| `calls` | 266,397 | 209,358 | -21% | 489 | 467 | -4% | -39% |
| `crc16` | 3,229,227 | 3,212,801 | -1% | 316 | 318 | +1% | -65% |
| `crc8` | 2,401,060 | 2,384,636 | -1% | 275 | 277 | +1% | -43% |
| `entities` | 1,274,992 | 1,013,257 | -21% | 897 | 833 | -7% | -44% |
| `fib` | 698,132 | 698,132 | +0% | 150 | 150 | +0% | -7% |
| `linkedlist` | 326,815 | 326,373 | -0% | 539 | 525 | -3% | -35% |
| `math16` | 4,335,342 | 4,334,740 | -0% | 1203 | 1197 | -0% | -20% |
| `oam` | 2,882,261 | 2,828,135 | -2% | 1201 | 1173 | -2% | -47% |
| `plasma` | 3,196,340 | 2,887,338 | -10% | 478 | 440 | -8% | -54% |
| `sieve` | 18,135,835 | 18,135,633 | -0% | 321 | 315 | -2% | -49% |
| `textprint` | 5,607,551 | 5,037,962 | -10% | 746 | 682 | -9% | -35% |

### 経過（2026-09-16、第 4 弾: C フラグ分岐とループ内の A / Y / X 常駐。第 3 弾 → 現在）

CRC の形（最上位ビットを検査して両方の枝で 1 ビットシフト）を「シフトしてから C で分岐」に（`if_carry`）、
ループごとに 1 バイト変数を A に 1 つ、添字 / カウンタを Y と X に 1 つずつ置いたまま回す割付
（[../doc/v2_regalloc.md](../doc/v2_regalloc.md)）。X を空けるためにスタックの空き先頭を X からゼロページの `FC_SP` に
移した（再帰関数と関数ポインタ経由の呼び出しに数サイクル乗る: `fib` +17%、`calls` +4%）。
crc8 の内側ループは `asl a; bcc; eor #29; dey; bne` になった。
その後、要素 2 バイトの配列の添字の 2 倍をブロック内で共有（`opt.scaleIndex`。math16 の `i*2` が Y に常駐）、
ループの外（関数の直線部分）も常駐の領域に（castle は 134 関数で引数などがレジスタに乗る。bench には効かない）、
`sta x; ldy x` → `tay` のピープホール（bgdecode はサイズが減ったのにサイクルが +0.6%: コードの配置が変わって
分岐がページをまたぐようになっただけで、生成コードの差は `tay` 化のみ）。
配列全体が 256 バイト以内なら `&objs[i]`（要素 3 バイト以上）の積を A だけの 8 ビットで計算（oam -11%、サイズ -24%）、
ピープホールで `sta x; …; lda x` の lda を次の命令が N/Z を立て直すなら消す（textprint -1%）。
可換な演算の第 2 入力が直前の命令の一時変数なら入れ替えて A に割り付くようにする（`opt.commuteTemp`。
`y + OFS_Y[i]` が `lda OFS_Y,x; clc; adc y` に。entities / oam -4%、crc8 -3.5%、bgdecode -2.4%）。

| bench | 第 3 弾 cycles | 現在 | | 第 3 弾 size | 現在 | | v0.0.2 比 |
|---|---:|---:|---:|---:|---:|---:|---:|
| `bgdecode` | 1,887,893 | 1,586,325 | -16% | 889 | 821 | -8% | -49% |
| `calls` | 210,345 | 216,095 | +3% | 457 | 476 | +4% | -37% |
| `crc16` | 3,212,795 | 2,508,088 | -22% | 310 | 297 | -4% | -72% |
| `crc8` | 2,384,632 | 1,352,072 | -43% | 271 | 258 | -5% | -68% |
| `entities` | 1,013,157 | 905,808 | -11% | 827 | 799 | -3% | -50% |
| `fib` | 698,132 | 815,200 | +17% | 150 | 176 | +17% | +9% |
| `linkedlist` | 326,333 | 326,333 | +0% | 513 | 513 | +0% | -35% |
| `math16` | 4,328,328 | 4,126,033 | -5% | 1191 | 1157 | -3% | -24% |
| `oam` | 2,853,035 | 2,198,935 | -23% | 1165 | 852 | -27% | -59% |
| `plasma` | 2,887,338 | 2,140,103 | -26% | 438 | 423 | -3% | -66% |
| `sieve` | 18,135,593 | 18,135,593 | +0% | 309 | 308 | -0% | -49% |
| `textprint` | 5,035,560 | 4,467,125 | -11% | 668 | 657 | -2% | -42% |

次は SSA 化（定数 / コピー伝播。volatile の仕様と一緒に）、2 バイト値の上位 / 下位の分割（sieve の 16 ビットのポインタ）。

補足（2026-09-15 の考察）:

- **cc65 はこの 10 年ほぼ速くなっていない**。公開ベンチの時系列: 2019 millfork-bench「cc65 2.18 は 2.16 より速くない」、
  2022 gglabs（2.18）sieve 23.1 s / crc8 3.3 s、2026 c-bench-64（2.19）20.5 s / 3.2 s。オプティマイザは 2.13（2009）から
  ほぼ同じ。fc を作った当初「cc65 より速い」と感じたのは、比較相手が `-O` 無し（3〜7 倍遅い。millfork の sieve は
  `cl65` 素で 6331 フレーム、`-Oirs` で 919）だったか、比べた処理が演算ループでなく関数呼び出し中心
  （cc65 は引数を `pusha` / `pushax` でソフトウェアスタックに積み、`char` が `int` に昇格する。fc の fastcall /
  X フレーム / 8 ビット型はそこを避ける設計）だったため、と考えられる
- c-bench-64 の C は cc65 に優しい書き方（全部 `unsigned char`、`register` ポインタ、`--static-locals`）で、
  ローカルが固定番地になると上の 1・4・5 を cc65 は自然に回避する
- 最近 10 年で本当に最適化が上がったのは SDCC の 6502 バックエンド（2022、4.2.0。木分解のレジスタ割付）、Oscar64（2021〜）、
  llvm-mos（2020〜）で、cc65 ではない
- 同じ 12 本を C で書いて手元の cc65 で fc と同じエミュレータで測る（B のゲーム形も比べられる）のは可能だが未実施。
  cc65 用のスタートアップと cfg、`*(char*)0xfffe = 4` の bench_start で半日程度

## ベンチを足すとき

1. `bench/<name>.fc` を書く（`main` の中で `bench_start()` … `bench_end()`、チェックサムを `printf`、`exit(0)`）
2. 別の言語で同じ計算をして答えを照合する
3. `go test ./bench -update` で `results.json` に登録し、この表に 1 行足す

## まだ無いもの

- **マクロベンチ**: `examples/castle` を `internal/nes` で N フレーム走らせて 1 フレームあたりの CPU サイクルを出す
  （`TestPlayCastle` の拡張。ゲーム全体の KPI）
- 他コンパイラとの直接比較（同じソースを cc65 / Oscar64 / llvm-mos に通して同じエミュレータで測る）
